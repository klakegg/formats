package net.klakegg.formats.mobi.content;

import net.klakegg.formats.common.util.ByteArrayReader;

public class DocumentContent extends AbstractContent {
    public DocumentContent(ByteArrayReader reader) {
        super(reader);
    }
}
